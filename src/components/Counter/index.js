const Counter=({counter})=>{
    // if (counter === 6) {
    //     // Simulate a JS error
    //     throw new Error(alert('I crashed!'));
    //   }
    
    return (
        <span id="count">0{counter} / 06</span>
    )    
}
export default Counter;